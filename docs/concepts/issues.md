---
id: issues
slug: /concepts/issues
title: Задачи в Datagent — статусы, план и декомпозиция
sidebar_label: Задачи
description: Задачи в Datagent — жизненный цикл, план агента, декомпозиция, результат и связь с артефактами.
---

# Задачи в Datagent

В Datagent любая работа агента начинается с **задачи**: один заголовок, один исполнитель, одна цепочка запусков и файлов. Не десять чатов в разных окнах — одна карточка на [app.datagent.ru](https://app.datagent.ru), куда вы возвращаетесь за статусом и результатом.

**Проект** группирует связанные задачи («Линия поддержки», «Продажи»). Задача может жить без проекта — для разовых поручений. Подробнее — [Проекты](./projects).

## Это работает так

1. **Создайте задачу** — заголовок и описание; при необходимости укажите проект.
2. **Назначьте агента** — у задачи всегда один исполнитель.
3. **Нажмите «Запустить»** — агент читает контекст задачи и приступает к работе.
4. **Следите за журналом** — при рискованном шаге откроется [согласование](./approvals).
5. **Заберите результат** — ответ в переписке или блоке результата; файлы — в [каталоге артефактов](/docs/artifacts/overview).

Пошаговый сценарий — [учебник «Одна задача»](/docs/guides/03-one-task).

## Статусы и приоритеты

| Статус | Что означает |
| --- | --- |
| **В очереди** | Задача заведена, агент ещё не взял |
| **К выполнению** | Готова к запуску |
| **В работе** | Агент выполняет или ждёт вас |
| **На проверке** | Результат ждёт проверки человеком |
| **Готово** | Закрыта, результат принят |
| **Заблокирована** | Стоп — ждёт вас, интеграции или бюджета |

Приоритеты **критический / высокий / средний / низкий** — чтобы команда видела, что горит первым.

:::tip Один агент на задачу
Нужны две роли — две задачи или родитель с **подзадачами** после разбиения плана.
:::

## План агента

Для крупной работы агент может сначала предложить **план** — документ в задаче со списком шагов. Вы **принимаете план** в панели; только после этого можно разбить работу на подзадачи (см. ниже).

1. Агент формирует план в задаче.
2. Вы подтверждаете план.
3. При необходимости **разбейте на подзадачи** — появляются дочерние задачи.
4. Каждую подзадачу ведёте отдельно: свой запуск, свой результат.

## Разбить задачу на подзадачи

Агент может сам разбить большую задачу на шаги. Доступно на тарифе **Studio** и выше.

**Пример:** поручение «Подготовь отчёт по продажам за квартал» → агент определяет шаги: собрать данные, сгруппировать, оформить документ → после принятия плана система создаёт **подзадачи** с тем же проектом и целью, что у родителя.

Для вас это выглядит как «разбить эпик на тикеты» без ручного копирования заголовков. Повторное разбиение того же плана не дублирует подзадачи.

:::info Тариф
На **Free** и **Solo** план можно принять, но автоматическое разбиение на подзадачи — с **Studio**. См. [тарифы](/docs/cloud/pricing).
:::

## Результат, вложения и артефакты

- **Переписка** — уточнения и итоговый текст.
- **Результат** — блок с итогами запуска в задаче.
- **Вложения** — файлы на задаче и в [каталоге артефактов](/docs/artifacts/overview).

## Задача, регламент и цель

| Сущность | Когда использовать |
| --- | --- |
| **Задача** | Разовое поручение с результатом |
| **Регламент** | То же по **расписанию** — [рутины](./routines) |
| **Цель** | Долгосрочный ориентир — [цели](./goals) |

## Частые вопросы

**Можно ли сменить агента?**  
Да, пока задача открыта — назначьте другого исполнителя. История запусков сохраняется.

**План не разбивается на подзадачи?**  
План должен быть **принят**; разбиение — на **Studio+**. Проверьте тариф и статус плана.

## Что дальше?

→ [Регламенты](./routines)
