---
id: issues
slug: /concepts/issues
title: Задачи в Datagent — статусы, план и декомпозиция
sidebar_label: Задачи
description: Задачи (issues) в Datagent — жизненный цикл, план агента, декомпозиция, Output и связь с артефактами.
---

# Задачи в Datagent

> **Зачем:** Чтобы поручение агенту жило в одном месте — с историей запусков, файлами и статусом, а не в десятке сообщений чата.

**Задача (issue)** — контейнер работы: формулировка, один исполнитель-агент, переписка, журнал run, вложения и **Output** (готовые результаты). На [app.datagent.ru](https://app.datagent.ru) вы открываете задачу по адресу `/{префикс}/issues/{номер}`.

## Это работает так

1. Вы создаёте задачу — заголовок, описание, при необходимости проект.
2. Назначаете **одного** агента (assignee) — в Datagent у задачи всегда один исполнитель.
3. Запускаете **Run / Wakeup** — агент читает контекст задачи и работает.
4. Смотрите шаги в журнале; при риске — [согласование](./approvals).
5. Забираете ответ из переписки или **Output**; файлы попадают в [каталог артефактов](/docs/artifacts/overview).

Пошаговый сценарий с картинками — в [учебнике «Одна задача»](/docs/guides/03-one-task).

## Статусы и приоритеты

| Статус | Что означает для вас |
| --- | --- |
| **backlog** | В очереди, агент ещё не взял в работу |
| **todo** | Готова к выполнению |
| **in_progress** | Агент работает или задача в активном цикле |
| **in_review** | Результат на проверке человеком |
| **done** | Закрыта, результат принят |
| **blocked** | Остановлена — ждёт вас, интеграции или бюджета |

Приоритеты: **critical**, **high**, **medium**, **low** — чтобы команда видела, что горит в первую очередь.

:::tip Один agent на задачу
Нужны две роли — две задачи или родитель с подзадачами после декомпозиции плана.
:::

## План агента и декомпозиция

Для крупной работы агент может сначала предложить **план** — документ в задаче с шагами. Вы **принимаете** план в панели; после этого доступна **декомпозиция**: система создаёт **подзадачи** по пунктам плана (идемпотентно — повторный запрос не дублирует детей).

**Это работает так:**

1. Агент в режиме планирования формирует plan-документ.
2. Вы подтверждаете план (accepted plan).
3. Запускаете декомпозицию — появляются дочерние задачи с тем же проектом/целью, что у родителя.
4. Каждую подзадачу можно вести отдельно: свой run, свой Output.

Для оператора это выглядит как «разбить эпик на тикеты» без ручного копирования заголовков.

## Output, вложения и артефакты

- **Переписка в задаче** — уточнения и итоговый текст ответа.
- **Output** — блок с результатами run (в том числе work products).
- **Вложения** — файлы на задаче; те же файлы видны в [каталоге артефактов](/docs/artifacts/overview) компании.

Агент загружает файлы через API во время run — см. [загрузку артефакта агентом](/docs/artifacts/agent-upload).

## Задача, рутина и цель

| Сущность | Когда использовать |
| --- | --- |
| **Задача** | Разовое поручение с конкретным результатом |
| **Рутина** | То же по **расписанию** — [рутины](./routines) |
| **Цель (goal)** | Долгосрочный ориентир — [цели](./goals) |

## Частые вопросы

**Можно ли сменить агента на лету?**  
Да, пока задача открыта — назначьте другого assignee. История run сохраняется.

**Где журнал запусков?**  
На карточке задачи и в разделе heartbeat-runs API — см. [обзор API](/docs/api-reference/overview).

**План не декомпозируется?**  
План должен быть **принят**; revision id должен совпадать с plan-документом задачи. Иначе API вернёт ошибку валидации.

## Что дальше?

- **Пройдите сценарий** — [одна задача](/docs/guides/03-one-task): от постановки до ответа
- **Настройте согласования** — [approvals](/docs/concepts/approvals): стоп-кран для рискованных действий
- **Откройте каталог** — [артефакты](/docs/artifacts/overview): все файлы агентов компании
- **Запланируйте повтор** — [рутины](/docs/concepts/routines): задачи по расписанию
