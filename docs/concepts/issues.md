---
id: issues
slug: /concepts/issues
title: Задачи в Datagent — статусы, план и декомпозиция
sidebar_label: Задачи
description: Задачи в Datagent — жизненный цикл, план агента, декомпозиция, результат и связь с артефактами.
---

# Задачи

Задача — это поручение агенту. Вы описываете, что нужно сделать, агент выполняет и возвращает результат. Задачу можно поставить вручную, через Телеграм, из Битрикс24 или автоматически по расписанию.

В панели на [app.datagent.ru](https://app.datagent.ru) у задачи один заголовок, один исполнитель и одна цепочка запусков — не разрозненные чаты.

**Проект** группирует связанные задачи («Линия поддержки», «Продажи»). Задача может жить без проекта — для разовых поручений. Подробнее — [Проекты](./projects).

## Это работает так

1. **Создайте задачу** — заголовок и описание; при необходимости укажите проект.
2. **Назначьте агента** — у задачи всегда один исполнитель.
3. **Нажмите «Запустить»** — агент читает контекст и приступает к работе.
4. **Следите за журналом** — при рискованном шаге откроется [одобрение](./approvals).
5. **Заберите результат** — ответ в переписке или блоке результата; файлы — в [каталоге артефактов](/docs/artifacts/overview).

Пошаговый сценарий — [учебник «Одна задача»](/docs/guides/03-one-task).

## Статусы задачи

| Статус | Что происходит |
| --- | --- |
| **Новая** | Задача создана, агент ещё не взялся |
| **В работе** | Агент выполняет задачу |
| **Ожидает одобрения** | Агент ждёт вашего решения |
| **Завершена** | Результат готов |
| **Остановлена** | Вы или система остановили задачу |

В панели могут отображаться уточняющие подписи («в очереди», «на проверке») — смысл тот же: задача ждёт запуска, агент работает, ждёт вас или уже закрыта.

Приоритеты **критический / высокий / средний / низкий** помогают команде видеть, что горит первым.

## Когда задача сложная

Некоторые задачи слишком большие для одного шага. На тарифе **Studio** и выше агент может сам разбить задачу на подзадачи и выполнять их по порядку. Вы видите всё дерево задач и контролируете каждую ветку.

Сначала агент предлагает **план** — список шагов в задаче. Вы **принимаете план** в панели; после этого можно разбить работу на подзадачи.

**Пример:** поручение «Подготовь отчёт по продажам за квартал» → агент определяет шаги: собрать данные, сгруппировать, оформить документ → после принятия плана появляются **подзадачи** с тем же проектом.

:::info Studio и выше
Декомпозиция задач доступна начиная с тарифа **Studio**. На Free и Solo план можно принять, но автоматическое разбиение на подзадачи — с Studio. [Тарифы →](/docs/cloud/pricing)
:::

## Результат, вложения и артефакты

- **Переписка** — уточнения и итоговый текст.
- **Результат** — блок с итогами запуска в задаче.
- **Вложения** — файлы на задаче и в [каталоге артефактов](/docs/artifacts/overview).

## Задача, регламент и цель

| Сущность | Когда использовать |
| --- | --- |
| **Задача** | Разовое поручение с результатом |
| **Регламент** | То же по **расписанию** — [рутины](./routines) |
| **Цель** | Долгосрочный ориентир — [цели](./goals) |

## Частые вопросы

**Можно ли сменить агента?**  
Да, пока задача открыта — назначьте другого исполнителя. История запусков сохраняется.

**План не разбивается на подзадачи?**  
План должен быть **принят**; разбиение — на **Studio+**. Проверьте тариф и статус плана.

## Что дальше

→ [Одобрения](./approvals) — как и когда агент просит вашего разрешения
