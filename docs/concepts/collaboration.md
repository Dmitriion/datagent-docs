---
id: collaboration
slug: /concepts/collaboration
title: Команда, приглашения и доступ
sidebar_label: Команда и доступ
description: Приглашения в компанию Datagent, роли owner/admin/operator/viewer, оргструктура агентов.
---

# Команда, приглашения и доступ

> **Зачем:** Подключить коллег к [app.datagent.ru](https://app.datagent.ru) с понятными правами — кто настраивает agent, кто запускает задачи, кто только смотрит отчёты.

В Datagent всё привязано к **компании**: человек и agent видят только свою организацию. Чужие данные закрыты и в панели, и в API.

## Роли людей в компании

| Роль | Обычно может |
| --- | --- |
| **owner** | Всё, включая биллинг и удаление компании |
| **admin** | Агенты, плагины, секреты, приглашения, бюджеты |
| **operator** | Задачи, run, согласования, просмотр агентов |
| **viewer** | Только чтение — без мутаций |

Точные права задаёт система **grants**; роль — удобная подсказка для типовых сценариев.

:::tip Viewer для руководителя
Дайте руководителю **viewer**, если он только смотрит журналы run — так он не сможет случайно изменить agent.
:::

## Приглашения

**Приглашение (invite)** — ссылка `/invite/{token}`: коллега входит в компанию без ручного создания учётки.

**Это работает так:**

1. **Admin** или **owner** создаёт приглашение в настройках компании (`POST /api/companies/:companyId/invites`).
2. Указывает тип (человек / агент), роль, срок действия, опционально onboarding-текст.
3. Отправляет ссылку коллеге.
4. Коллега проходит `/invite/…`, принимает условия — получает **membership** в компании.
5. Приглашение можно **отозвать** до принятия — `POST /api/invites/:inviteId/revoke`.

Полный список маршрутов — [доступ (API)](/docs/api-reference/access).

Без входа в систему доступны метаданные приглашения: `GET /api/invites/:token` — название компании, роль, срок.

Первый владелец появляется при [регистрации](/docs/cloud/getting-started); invite — для второго и следующих участников.

## Участники компании

- Список: `GET /api/companies/:companyId/members`
- Изменение роли / статуса — через API доступа (board).
- Удаление участника — с проверкой иерархии ролей (нельзя удалить равного или старшего без прав).

Люди и agent в оргструктуре — разные сущности; в одной таблице «сотрудников» их не смешивают.

## Оргструктура агентов

Карта AI-сотрудников для отчётов и демо руководству:

- `GET /api/companies/:companyId/org` — JSON-дерево
- `GET …/org.svg` / `…/org.png` — картинка для отчётов

Полезно для демо руководству: кто кому подчиняется, какие роли в [Офисе](/docs/office/overview).

## Агенты и API-ключи

- **Ключ agent** — только свой профиль, wakeup и узкий inbox; чужие секреты и компании недоступны.
- **Сессия панели** — полный доступ board в рамках Вашей роли.

Подробнее о ключах — [агенты (API)](/docs/api-reference/agents).

## Согласования и inbox

Operator видит [входящие](./inbox) и [согласования](./approvals). **Viewer** — только просмотр, без approve.

## Частые вопросы

**Ссылка invite истекла?**  
Создайте новое приглашение; старые токены имеют `expiresAt`.

**Коллега видит чужую компанию?**  
Нет — membership привязан к одной организации; переключение компании в панели, если у человека несколько.

**Кто может приглашать?**  
Обычно **admin** / **owner** с правом `users:invite`.

## Что дальше?

- [Настроить аккаунт и компании](/docs/cloud/account) — переключение организаций в шапке
- [Разобраться с секретами](/docs/concepts/secrets) — кто может менять ключи
- [Создать первого agent](/docs/cloud/first-agent) — после приглашения operator
- [Узнать, кто одобряет рискованные шаги](/docs/concepts/approvals)
