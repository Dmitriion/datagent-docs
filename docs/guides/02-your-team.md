---
title: Как собрать команду агентов, которая не срывает сроки
sidebar_label: '2. Команда агентов'
description: "Агенты Datagent как роли с моделью и tools — история Алексея: команда, а не разрозненные чаты."
sidebar_position: 3
---

**Алексей** отвечает за операционку и инициативу по агентам. Команда хочет «одного на всё» — без рамок получается хаос: один лезет в браузер, другой пишет простыни, третий не видит политику компании. Здесь вы соберёте **команду ролей** с понятными границами.

## Было и стало

| Было | Стало |
| --- | --- |
| Один универсальный промпт | Несколько **агентов Datagent** под сценарии |
| Все tools всем | Только включённые **plugin tools** |
| Непонятно, кто в работе | Статусы run и список агентов |

## Как завести трёх агентов за неделю

**Шаг 1.** Договоритесь с инженером: instance поднят, [плагины](../tutorials/build-plugin) установлены (Bitrix24, Телеграм, при необходимости Office Plugin).

**Шаг 2.** Создайте **«Аналитик переписки»** — адаптер `gigachat_local` или `yandexgpt_local`, короткий system prompt: «русский, структура, без выдуманных цифр».

**Шаг 3.** Второй агент — **«Оформитель таблиц»**: те же LLM, в tools только `datagent.excel-workbench:*` после [Office Plugin](../office/excel-pptx).

**Шаг 4.** Третий — **«Внутренний исследователь»** с [BrowserBridge](../tutorials/browserbridge-setup): tools `datagent.browserbridge:browser_*`, правило «не отправлять формы без одобрения».

**Шаг 5.** Секреты (`GIGACHAT_*`, `YANDEX_SA_KEY_JSON`) — через **secret_ref** компании, не в тексте задачи.

**Шаг 6.** Покажите оператору: он **не меняет** adapter, он **запускает** нужного агента в задаче.

**Шаг 7.** В пятницу откройте список агентов: у кого последний run `failed` — разбор с инженером, не «перезагрузи страницу».

![Список агентов компании](/img/guides/team/agents-list.webp)

*Список агентов — кто в ростере, какая модель и когда был последний run.*

```mermaid
flowchart TB
  subgraph Roles [Роли Алексея]
    A1[Аналитик переписки]
    A2[Оформитель таблиц]
    A3[Исследователь BrowserBridge]
  end
  subgraph Limits [Ограничения]
    LLM[Адаптер и модель]
    Tools[Только выданные tools]
    Appr[Одобрения на риск]
  end
  Roles --> Limits
```

## Где вы управляете возможностями

Вы задаёте не только промпт, но и **границы**: агент не вызовет tool, которого нет в manifest плагина. Нет вымышленных `bitrix24_list_leads` — только сценарии из [Bitrix24](../integrations/bitrix24).

## Что не стоит делать на старте

:::warning
- Выдать BrowserBridge и Office всем «на всякий случай» — вырастет очередь одобрений.
- Писать секреты в задачу или в system prompt.
- Ждать, что агент «сам найдёт» 1С — нужен [коннектор](./08-1c-bridge) и MCP в IDE, не tool в heartbeat.
:::

## Быстрая победа за 5 минут

:::tip
Создайте одного агента с правилом в prompt: «Отвечай только списком из 5 пунктов». Запустите wakeup — проверьте, что граница держится.
:::

## Что дальше

**Следующая глава:** [Одна задача — один результат](./03-one-task)

- [Первый агент](../getting-started/first-agent)
- [LLM-адаптеры](../concepts/llm-adapters)
