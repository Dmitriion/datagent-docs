# Dependencies
/node_modules

# Production
/build

# Generated files
.docusaurus
.cache-loader

# Misc
.DS_Store

# Environment and secrets (never commit)
.env
.env.*
!.env.example
.env.local
.env.development.local
.env.test.local
.env.production.local

# Playwright / screenshot automation (may contain session cookies)
**/datagent-board-auth.json
**/*storageState*
playwright/.auth/

npm-debug.log*
yarn-debug.log*
yarn-error.log*

scripts/capture-guide-screenshots-report.json

# Security — keys & credentials (P1 security fix)
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer
id_rsa
id_ed25519
secrets.json
credentials.json
serviceAccount*.json
*-service-account.json
google-credentials.json
